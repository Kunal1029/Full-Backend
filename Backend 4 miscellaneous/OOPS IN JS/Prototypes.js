console.log("Prototype - Every object in js has a built-in property, which is called its prototype")
console.log("Prototype has its own prototype , which called chain prototype. The chain ends when we reach a prototype that has null for its own prototype")

console.log("Object inherit prototype , prototype which includes all properties")
// So object will inherit all info inside prototype 

//It is like a single template object that all object inherit methods and properties from without having their own copy.


// arr._proto_(reference)
// Array.prototype(actual object)
// String.prototype